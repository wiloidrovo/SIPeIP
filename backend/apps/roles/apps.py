from django.apps import AppConfig


class RolesConfig(AppConfig):
    """Configuración de la aplicación de roles."""
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.roles"
    verbose_name = "Roles"